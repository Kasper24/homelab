apiVersion: k3d.io/v1alpha4
kind: Simple

metadata:
  name: ${cluster_name}

servers: ${server_count}
agents: ${agent_count}

ports:
  - port: "80"
    nodeFilters:
      - loadbalancer
  - port: "443"
    nodeFilters:
      - loadbalancer

registries:
  create:
    name: dev
    hostPort: "5000"
